# Agent 执行契约

1. 先读README.md、docs/ 下的说明和 templates/ 下的模板。不要访问上级目录，也不要寻找原作者的服务器或账号。
2. 首轮集中询问预算/现有 VPS、所在地与运营商、系统、是否有原生 IPv6、已有域名和管理方式。公共教程不含购买授权；付费前给出总价、续费和退款条款并获得同意。
3. 记录当前软件版本。优先官方文档，不把旧版本配置、价格、面板字段和安装命令当作现行事实。使用官方发布资产，执行安装脚本前下载审阅；不运行仓库之外的未知一键脚本。
4. 先备份已有服务、端口占用和防火墙规则。SSH 保持当前会话，验证第二个会话能登录后再收紧访问。不要把新部署覆盖到已有业务上。
5. 先完成 HY2 单节点端到端成功，再加入独立 Xray、CDN、个人订阅和客户端脚本。每阶段验证并记录；失败时保留上一阶段可用入口。
6. 密码、UUID、订阅 token、DNS API token 由使用者自行生成；不要求在聊天中公开。个人凭据和仅由所有者读取的文件默认 600；root 管理且服务必须读取的配置/私钥可用 root:vpsproxy、文件 640、目录 750，并确认该组仅含必要服务身份、父目录可遍历。按实际服务用户验证可读性，不给其他用户读取权限。DNS token 最小授权；面板仅本机监听，经 SSH 隧道管理优先。
7. IPv6 不可用时不生成 IPv6 节点。UDP 不通时先排查供应商策略和路径，不能用 TCP 端口探测代替 HY2 验收。
8. 使用有效 TLS 证书，SNI 必须匹配；不要为消除报错关闭证书校验。不要套用旧 auth 数据库补丁或自动修改用户数据库。
9. 不声称自动回退会重试所有已失败请求，不承诺同等网速。不得只凭 HTTP 首页或控制 API 200 宣称代理认证成功。
10. 完成后整理使用者的私密运行配置、独立订阅、恢复方法与验收记录。未完成项明确标记，不能将仓库的离线测试等同于生产部署。
11. 修改文档后人工通读受影响段落：中文使用短句、主动语态和统一术语，避免“本包”“接收者”等内部称呼，以及明显的机器翻译腔。不要把没有实测的能力写成承诺；文案检查不能只看脚本是否通过。
12. 修改中文源文后同步更新对应的多语言校验哈希，并运行 `python3 tests/check_copy.py`、`python3 tests/check_docs.py`、`python3 tests/check_i18n.py` 和 `node --test tests/*.test.js`。多语言页面只写源文档已确认的事实，未经过母语人工审阅时保持现有提示。
